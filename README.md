# Blogify API

Initial scaffold for the Blogify API.

Run locally:

```powershell
npm install
npm run dev    # use nodemon for development
npm start      # production run
```

Server listens on port 5000 by default (or set PORT env var).
Blogify API

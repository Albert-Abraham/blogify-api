// routes/index.js
// placeholder for route registration

module.exports.register = function registerRoutes(app) {
  // In a real app you'd wire controllers to paths here.
  // This is a placeholder to show exported functions from routes folder.
  return {
    info: 'routes placeholder'
  };
};
